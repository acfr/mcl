# <Message name> = <data rate in Hz>, <data size in bytes>
# ---------------------------------------------------------

ImuMessage = This is a bad format because the line is not 
             a comment contains a valid message name, has
             the delimiter character but no valid 
             parameters.


